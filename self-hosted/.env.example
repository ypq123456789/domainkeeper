# DomainKeeper 环境变量配置
# 复制此文件为 .env 并根据您的需求修改配置

# JWT 密钥 (必须修改)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# 管理员账户配置
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# 前台访问密码 (可选，留空表示不需要密码)
ACCESS_PASSWORD=

# Cloudflare API Token (可选，用于自动同步域名)
CF_API_TOKEN=

# WHOIS 代理服务地址 (可选，用于获取域名WHOIS信息)
WHOIS_PROXY_URL=https://whois.0o11.com

# 数据同步间隔 (分钟)
SYNC_INTERVAL=60

# 前端访问地址 (用于CORS配置)
FRONTEND_URL=http://localhost

# 前端端口 (默认80)
FRONTEND_PORT=80

# API 地址 (前端配置)
VUE_APP_API_BASE_URL=http://localhost:3001/api

# 自定义标题
CUSTOM_TITLE=我的域名管理

# 日志级别 (error, warn, info, debug)
LOG_LEVEL=info
